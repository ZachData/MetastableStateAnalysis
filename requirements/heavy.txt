# heavy tier — real forward passes, clustering, figures.
#
# Install torch from the CPU index, NOT plain PyPI:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install -r requirements/heavy.txt
# The plain PyPI wheel is the CUDA build (~4.9 GB unpacked) and nothing in CI
# needs a GPU.
-r test.txt
torch>=2.0
# Upper bound is load-bearing, not hygiene. transformers 5 moved GPTNeoX's
# rotary geometry from config.rotary_pct / config.rotary_emb_base into
# config.rope_parameters, and core/rope.py reads the old names with a
# `getattr(cfg, "rotary_pct", 1.0)` fallback. Under 5.x that default fires and
# Pythia's frame card reports rotary_ndims = head_size (64 on pythia-70m)
# instead of the 16 the model actually rotates -- silently, with every
# RoPE-dependent number downstream of it wrong. Measured on 5.16.1; the smoke
# tier is the only tier that sees it, because every rope test builds a fake
# config using the pre-5 attribute names.
# Lift this bound only together with a core/rope.py that reads rope_parameters.
transformers>=4.35,<5
matplotlib>=3.7
scikit-learn>=1.3
# Load-bearing, not optional colour. CLAIM-C's registered metric set takes two
# of its six metrics -- cluster_count and cluster_membership -- from HDBSCAN
# and from nothing else, and p1_mstate_tracking/clustering.py skips HDBSCAN
# silently when this import fails. It went missing from the working venv
# between 2026-08-12 and 2026-08-31 while unnamed here, and every Phase 1 run
# made in that window is unscoreable by that gate as a result.
hdbscan>=0.8.40
