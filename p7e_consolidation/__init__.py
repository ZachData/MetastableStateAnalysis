"""
p7e_consolidation — `L11H14`, the member that does the same job from an
orthogonal subspace; and the question it raises: can the redundancy set be
*collapsed* into a single head without losing the behaviour?

Two objects, one phase, because they are the same question from both ends.
`L11H14` is the extreme case of "same function, different subspace" (7d found it
at mean cosine −0.033 to the set while holding interaction +1.53), and
consolidation asks whether that difference is load-bearing or merely
incidental — if a single head can be made to span what the set held, the
distinctness was incidental.

See design-7e.md for the design, the capacity argument, and what would falsify
it. Nothing here is registered: pythia-410m is spent under `check_registry`
rule 3, and a consolidated model is a *modified* artifact, which is a second and
independent reason no claim from it can be registered against the original.

Depends on 7d: `p7d_redundancy/` defines membership causally and supplies the
interaction matrix and the geometry this phase starts from. Read
`p7d_redundancy/status-7d.md` first.
"""
